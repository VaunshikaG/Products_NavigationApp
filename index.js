import { AppRegistry } from 'react-native';
import { name as appName } from './app.json';
import ProductNavigationApp from './src/App';

AppRegistry.registerComponent(appName, () => ProductNavigationApp);
